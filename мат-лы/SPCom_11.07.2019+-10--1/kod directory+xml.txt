				object fileName = Directory.GetCurrentDirectory();
				fileName += "\\Справка.doc";

---------------------------------------------------------------------------------------------------------------------------------------------------------------------------
						if (!char.IsDigit(e.KeyChar) )---------должны быть введены только цифры
						{
							errorProvider1.SetError(txtPIN, "Must be number");
							lbloutput.Text ="Поля PIN и PIN2 не могут содержать буквы";
						}
----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------